services:
  - type: web
    name: bias-news-api
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/app.py
    envVars:
      - key: FLASK_ENV
        value: production
    rootDir: backend  # Ensure it's pointing to the correct backend folder
